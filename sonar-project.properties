# Project
sonar.projectKey=HxCFD
sonar.projectName=HxCFD
sonar.projectVersion=1.0

# SonarQube Server
sonar.host.url=http://localhost:9000

# Source code
sonar.sources=.

# Encoding
sonar.sourceEncoding=UTF-8

# Ignore common generated/dependency folders
sonar.exclusions=**/.git/**,**/node_modules/**,**/__pycache__/**,**/.venv/**,**/venv/**,**/dist/**,**/build/**,**/.next/**,**/coverage/**

# Optional: reduce SCM warnings if needed
# sonar.scm.provider=git